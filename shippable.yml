# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  
env:
  global:
    - FOO=FUBU

  matrix:
    include:
      - node_js: 0.12
        env: M1=ONE

build:

  ci:
    - ps -eax
    - ls -al
